import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Providers } from "./providers";

const inter = Inter({ subsets: ["latin"], variable: "--font-inter" });

export const metadata: Metadata = {
  title: "MeetMind — AI Meeting Intelligence",
  description:
    "Every meeting, remembered. Every action, tracked. MeetMind turns every call into searchable notes, clear summaries, and tracked tasks — automatically.",
  keywords: ["meeting intelligence", "AI transcription", "meeting summaries", "action items"],
  authors: [{ name: "MeetMind" }],
  openGraph: {
    title: "MeetMind — AI Meeting Intelligence",
    description: "Every meeting, remembered. Every action, tracked.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <head>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="anonymous" />
        <link
          href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=JetBrains+Mono:wght@400;500&display=swap"
          rel="stylesheet"
        />
      </head>
      <body className={`${inter.variable} antialiased`}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
